// Espera o DOM carregar
document.addEventListener('DOMContentLoaded', function () {

    // Seleciona a modal
    const minhaModal = new bootstrap.Modal(document.getElementById('minhaModalExemplo'));

    // Abre a modal automaticamente
    minhaModal.show();

});